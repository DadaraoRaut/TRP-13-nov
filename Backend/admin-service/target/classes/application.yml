server:
  port: 8086
  error:
    include-message: always

spring:
  application:
    name: ADMIN-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/admin_service_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: quantumsoft123@gmail.com
    password: "ydbd qxmj coxw khtw"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

# Optional: Logging for debugging JWT / requests
logging:
  level:
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org:
      springframework:
              security: DEBUG

auth:
  service:
    url: http://localhost:8081/api/auth
# application.yml
jwt:
  secret: mySuperSecretKey123456789012345678901234567890
